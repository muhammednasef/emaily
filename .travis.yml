sudo: required

services:
  - docker

before_install:
  - docker build -t me/emaily -f ./server/Dockerfile ./server

script:
  - docker run me/emaily npm run test

notifications:
  email:
    on_success: always
    on_failure: always
    recipients:
      - muhammed.nasef16@gmail.com

after_success:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push muhammedkamel/emaily

deploy:
  skip_cleanup: true
  provider: script
  script: echo "deployment successful...."
